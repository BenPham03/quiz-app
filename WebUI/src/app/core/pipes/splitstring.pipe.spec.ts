import { SplitstringPipe } from './splitstring.pipe';

describe('SplitstringPipe', () => {
  it('create an instance', () => {
    const pipe = new SplitstringPipe();
    expect(pipe).toBeTruthy();
  });
});
